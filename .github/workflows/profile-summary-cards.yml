name: GitHub Profile Summary Cards

on:
  push:
    branches: [main]
    paths-ignore:
      - 'assets/**'
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PROFILE_SUMMARY_CARDS }}
      - uses: vn7n24fzkq/github-profile-summary-cards@release
        env:
          GITHUB_TOKEN: ${{ secrets.PROFILE_SUMMARY_CARDS }}
        with:
          USERNAME: Kevin-Perdomo
          UTC_OFFSET: -3
          AUTO_PUSH: false
      - name: Push to main
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          mkdir -p assets/summary
          cp -r profile-summary-card-output/dark/. assets/summary/
          git add assets/summary/
          git diff --cached --quiet || (git commit -m "update profile summary cards" && git pull --rebase origin main && git push origin main)
